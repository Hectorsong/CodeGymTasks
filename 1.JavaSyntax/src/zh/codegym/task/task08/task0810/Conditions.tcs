taskKey="zh.codegym.task.task08.task0810"\n\n执行 10000 次 get 调用所需的时间

测量对每个列表执行 10000 次 get 和 set 调用所需的时间。
getGetTimeInMs 方法必须返回其执行时间，以毫秒表示。


Requirements:
1.	程序应在屏幕上显示数字。
2.	main 方法只能调用两次 getGetTimeInMs 方法。
3.	main 方法只能调用两次 fill 方法。
4.	fill(List list) 方法必须将 10000 个项目插入列表中。
5.	get10000(List list) 方法应对列表调用 10000 次 get。
6.	getGetTimeInMs 方法只能调用一次 get10000(List list) 方法。
7.	getGetTimeInMs 方法应返回对列表执行 10000 次 get 调用所需的时间（以毫秒表示）。


